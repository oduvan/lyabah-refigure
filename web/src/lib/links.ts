/** Canonical outbound links, kept in one place so they stay consistent. */
export const REPO_URL = 'https://github.com/oduvan/refigure-cli'
export const APP_REPO_URL = 'https://github.com/oduvan/refigure'
// The Store listing is a product page, not a release asset: it does not carry
// a version and never moves, so unlike the installers it is not served from
// /api/v1/releases.
export const STORE_URL = 'https://apps.microsoft.com/detail/9N9914T7RTJ6'
